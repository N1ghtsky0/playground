<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>Title</title>
  <meta name="_csrf" content="{{_csrf.token}}"/>
  <meta name="_csrf_header" content="{{_csrf.headerName}}"/>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous" />

  <style>
    img.login-icon {
      min-width: 24px;
      min-height: 24px;
    }
  </style>
</head>
<body>
<div class="container">
  <h1 class="my-3">로그인 페이지</h1>
  <form class="border mb-3" method="post" id="frm">
    <input type="hidden" name="{{_csrf.parameterName}}" value="{{_csrf.token}}">
    <div class="m-3">
      <div class="mb-3">
        <label for="loginId" class="form-label">아이디</label>
        <input type="text" class="form-control mb-1" id="loginId" name="loginId">
      </div>
      <div class="mb-3">
        <label for="password" class="form-label">비밀번호</label>
        <input type="password" class="form-control" id="password" name="password">
      </div>
      <div class="d-flex justify-content-end">
        <button type=submit class="btn btn-primary" id="submitBtn">로그인</button>
      </div>
    </div>
  </form>
  <div class="d-flex justify-content-evenly">
    <a href="/oauth2/authorization/google">
      <img class="login-icon" src="/static/images/google-icon.svg" alt="google-icon">
    </a>
  </div>
</div>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
</body>
</html>